# MongoDB
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/tamin?retryWrites=true&w=majority

# Auth
AUTH_SECRET=replace-with-a-long-random-string

# Public site URL — used in metadata, sitemap, robots, canonical tags
NEXT_PUBLIC_SITE_URL=https://tamin-market.ir
